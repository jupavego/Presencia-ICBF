# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Credenciales de Supabase — nunca comitear
.env
.env.*
!.env.example

# Salida local del seed de demo (documentos generados, datos simulados) —
# ver scripts/seed-demo.mjs
scripts-output/

# Llave de la cuenta de servicio de Google Drive — nunca comitear. Vive
# aquí solo temporalmente mientras se sube como secreto a Supabase; una
# vez ahí, este archivo local se puede borrar.
*.iam.gserviceaccount.json
presencia-icbf-*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
